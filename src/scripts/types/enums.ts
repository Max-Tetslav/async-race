export enum Events {
  input = 'input',
  click = 'click',
  load = 'load',
  hashchange = 'hashchange',
}

export enum Methods {
  get = 'get',
  post = 'post',
  put = 'put',
  delete = 'delete',
}
