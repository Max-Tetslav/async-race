export enum Events {
  input = 'input',
  click = 'click',
  load = 'load',
  hashchange = 'hashchange',
}

export enum Methods {
  get = 'GET',
  post = 'POST',
  put = 'PUT',
  delete = 'DELETE',
  patch = 'PATCH',
}

export enum EngineStates {
  start = 'started',
  stop = 'stopped',
  drive = 'drive',
}
